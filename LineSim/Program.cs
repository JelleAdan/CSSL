using CSSL.Examples.AssemblyLineOld;
using CSSL.Modeling;
using System;

namespace LineSim
{
    class Program
    {
        static void Main(string[] args)
        {
            Simulation sim = new Simulation("AssemblyLineSimulation", @"C:\CSSLtest");

            //AssemblyLine assemblyLine = new AssemblyLine(sim.MyModel, "AssemblyLine", )
        }
    }
}
